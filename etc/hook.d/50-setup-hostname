#!/bin/sh
#
#  This script places the new systems hostname into a couple of files within
# the new image.
#
# Steve
# --
# $Id: 50-setup-hostname,v 1.3 2005-12-25 02:16:00 steve Exp $



prefix=$1

echo ${hostname} > ${prefix}/etc/hostname
echo ${hostname} > ${prefix}/etc/mailname
